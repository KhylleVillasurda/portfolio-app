{
  "buildCommand": "pnpm --filter showcase-portfolio build",
  "outputDirectory": "frontend/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
