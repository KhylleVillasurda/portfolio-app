{
  "name": "portfolio-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter showcase-portfolio dev",
    "dev:backend": "cd backend && cargo run",
    "install:all": "pnpm install",
    "typecheck": "pnpm --recursive typecheck"
  }
}
